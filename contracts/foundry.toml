# File: contracts/foundry.toml
[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
via_ir = true
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
rollup_a = "${ROLLUP_A_RPC}"
settlement = "${SETTLEMENT_RPC}"
rollup_b = "${ROLLUP_B_RPC}"
